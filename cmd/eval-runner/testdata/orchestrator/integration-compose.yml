services:
  healthy:
    image: busybox:1.36
    command:
      - sh
      - -c
      - touch /tmp/healthy && tail -f /dev/null
    healthcheck:
      test:
        - CMD
        - test
        - -f
        - /tmp/healthy
      interval: 1s
      timeout: 1s
      retries: 10
